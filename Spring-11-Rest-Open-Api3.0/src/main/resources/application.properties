spring.datasource.url=jdbc:postgresql://localhost:5432/cybertek
spring.datasource.username=postgres
spring.datasource.password=Kiraoct23
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=create

!- we need this to add data from queries dattasql
spring.datasource.initialization-mode=always

!- to customize api path to documentation
springdoc.api-docs.path=/api-docs

!- if one of the entities is missing // do not pick up any values which are not described, {"hibernateLazyInitializer"} - whenever fetch is lazy it will add fieled hibernateinitilizer, if we do not want this fieled witthout it it will not run
!- @JsonIgnoreProperties(value ={"hibernateLazyInitializer"},ignoreUnknown = true)
spring.jackson.serialization.fail-on-empty-beans=false