spring.datasource.url=jdbc:postgresql://localhost:5432/cybertek
spring.datasource.username=postgres
spring.datasource.password=Kiraoct23
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=create

!- we need this to add data from queries dattasql
spring.datasource.initialization-mode=always



!- if we do not put lazy serialazation ignore project will still run , we can solve it on a class level or in a property file
spring.jackson.serialization.fail-on-empty-beans=false


